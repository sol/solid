cradle:
  bios:
    shell: "solid ghc-options -Wno-overlapping-patterns -package=doctest -package=markdown-unlit -package=bytestring -package=text -pgmL=markdown-unlit $HIE_BIOS_ARG > $HIE_BIOS_OUTPUT"
